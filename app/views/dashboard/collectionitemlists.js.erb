$("#load_more_contracts").append("<%=j render 'contracts' %>");
<% if @own_contract.next_page.present? %>
  $("#load_more_contract").attr("href", "<%= owncollections_path(page: @own_contract.next_page) %>");
<% else %>
  $(".load_more_section").remove();
<% end %>
