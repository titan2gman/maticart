$("#user_notification").append("<%=j render 'dashboard/all_notifications' %>");
<% if @notifications.next_page.present? %>
  $("#load_more_activity").attr("href", "<%= all_notifications_path(page_no: @notifications.next_page) %>");
<% else %>
  $(".load_more_section").remove();
<% end %>
